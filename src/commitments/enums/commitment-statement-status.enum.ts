export enum CommitmentStatementStatus {
  DRAFT = 'draft',
  SUBMITTED = 'submitted',
  AWAITING_SIGNATURES = 'awaiting_signatures',
  SIGNED = 'signed',
  SUPERSEDED = 'superseded',
  CANCELLED = 'cancelled',
}
